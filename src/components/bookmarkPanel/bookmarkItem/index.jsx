import PropTypes from "prop-types";

const BookmarkItem = ({ name, url, icon, id }) => {
    return <>İtem</>;
};
BookmarkItem.propTypes = {
    name: PropTypes.string,
    url: PropTypes.string,
    icon: PropTypes.string,
    id: PropTypes.string,
};
export default BookmarkItem;
